INCLUDE_DIRECTORIES(include)
SET(SRC
        src/main.cpp)

FILE(COPY input DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
FILE(COPY out DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

ADD_EXECUTABLE(pizzaSlicer6000 ${SRC})

